/**
 * _strlen - prints length of string
 * @s: string to be examined
 *
 * Return: returns the length of the string
 */
int _strlen(char *s)
{
	int i; 

	for (i = 0; i != '\0'; ++i)
	{
		;
	}
	return (i);
}
